apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "simple-site-chart.fullname" . }}
  labels:
    app: {{ include "simple-site-chart.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "simple-site-chart.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "simple-site-chart.name" . }}
    spec:
      containers:
        - name: {{ include "simple-site-chart.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 80

