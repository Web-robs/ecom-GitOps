apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-site-chart.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: {{ include "simple-site-chart.name" . }}

